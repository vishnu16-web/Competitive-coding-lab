Program:

include <stdio.h>

nsigned int hamming Distance(unsigned

nt x, unsigned int y)

Insigned int dist = 0;

Insigned int val = x^ y; // bitwise XOR of x and y

while (val) {

/ count the number of set bits (i.e., 1s) in

al dist++;

al &= val - 1;

eturn dist;

nt main() {

unsigned int x = 10; unsigned int y = 15;

printf("%u\n", hamming Distance(x, y));

return 0;
